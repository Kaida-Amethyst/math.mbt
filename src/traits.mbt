///|
pub trait Floating {
  sin(Self) -> Self
  cos(Self) -> Self
  tan(Self) -> Self
  asin(Self) -> Self
  acos(Self) -> Self
  atan(Self) -> Self
  sinh(Self) -> Self
  cosh(Self) -> Self
  tanh(Self) -> Self
  asinh(Self) -> Self
  acosh(Self) -> Self
  atanh(Self) -> Self
}
